// import { redis } from "@/db";

// const getJSON = async <T>(key: string): Promise<null | T> => {
//   const data = await redis.json.get(key);

//   if (!data) return null;

//   return data as T;
// };

// export { getJSON };
