import { css } from "@/theme";

export const wrapperClass = css({
  alignContent: "start",
  alignItems: "start",
  display: "grid",
  gridAutoFlow: "row",
  gridTemplateRows: "auto 1fr",
  height: "100%",
});
