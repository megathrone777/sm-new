import { style } from "@/theme";

export const wrapperClass = style({
  alignContent: "start",
  alignItems: "start",
  display: "grid",
  gridAutoFlow: "row",
  gridTemplateRows: "auto 1fr",
  height: "100%",
});
