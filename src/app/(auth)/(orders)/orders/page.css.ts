import { css } from "@/theme";

export const wrapperClass = css({
  height: "100%",
  padding: 10,
});

export const listClass = css({
  columnGap: 10,
  display: "flex",
  flexWrap: "wrap",
  rowGap: 10,
});
