import { css } from "@/theme";

export const wrapperClass = css({
  display: "grid",
  gridAutoFlow: "row",
  justifyContent: "center",
  rowGap: 8,
});

export const imageClass = css({
  borderRadius: 8,
  objectFit: "cover",
});
